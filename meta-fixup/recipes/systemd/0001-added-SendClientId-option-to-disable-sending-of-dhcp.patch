From 57271217aaa8228b895b80d00147a29ce83f161c Mon Sep 17 00:00:00 2001
From: Enrico Scholz <enrico.scholz@sigma-chemnitz.de>
Date: Tue, 14 Oct 2014 15:23:44 +0200
Subject: [PATCH] added SendClientId option to disable sending of dhcp option
 61

Signed-off-by: Enrico Scholz <enrico.scholz@sigma-chemnitz.de>

Conflicts:
	src/network/networkd-network.c
---
 src/libsystemd-network/sd-dhcp-client.c  | 12 +++++++++++-
 src/network/networkd-dhcp4.c             |  5 +++++
 src/network/networkd-network-gperf.gperf |  1 +
 src/network/networkd-network.c           |  1 +
 src/network/networkd-network.h           |  1 +
 src/systemd/sd-dhcp-client.h             |  1 +
 6 files changed, 20 insertions(+), 1 deletion(-)

diff --git a/src/libsystemd-network/sd-dhcp-client.c b/src/libsystemd-network/sd-dhcp-client.c
index 62099dd..2a0dd56 100644
--- a/src/libsystemd-network/sd-dhcp-client.c
+++ b/src/libsystemd-network/sd-dhcp-client.c
@@ -58,6 +58,7 @@ struct sd_dhcp_client {
         union sockaddr_union link;
         sd_event_source *receive_message;
         bool request_broadcast;
+        bool skip_client_id;
         uint8_t *req_opts;
         size_t req_opts_allocated;
         size_t req_opts_size;
@@ -139,6 +140,15 @@ int sd_dhcp_client_set_request_broadcast(sd_dhcp_client *client, int broadcast)
         return 0;
 }
 
+int sd_dhcp_client_set_send_client_id(sd_dhcp_client *client, int send)
+{
+        assert_return(client, -EINVAL);
+
+        client->skip_client_id = !send;
+
+        return 0;
+}
+
 int sd_dhcp_client_set_request_option(sd_dhcp_client *client, uint8_t option) {
         size_t i;
 
@@ -470,7 +480,7 @@ static int client_message_init(sd_dhcp_client *client, DHCPPacket **ret,
                 memcpy(&packet->dhcp.chaddr, &client->mac_addr, ETH_ALEN);
 
         /* If no client identifier exists, construct an RFC 4361-compliant one */
-        if (client->client_id_len == 0) {
+        if (client->client_id_len == 0 && !client->skip_client_id) {
                 size_t duid_len;
 
                 client->client_id.type = 255;
diff --git a/src/network/networkd-dhcp4.c b/src/network/networkd-dhcp4.c
index 03c28bb..465f464 100644
--- a/src/network/networkd-dhcp4.c
+++ b/src/network/networkd-dhcp4.c
@@ -544,6 +544,11 @@ int dhcp4_configure(Link *link) {
         if (r < 0)
                 return r;
 
+        r = sd_dhcp_client_set_send_client_id(link->dhcp_client,
+                                              link->network->dhcp_send_client_id);
+        if (r < 0)
+                return r;
+
         r = sd_dhcp_client_set_mac(link->dhcp_client,
                                    (const uint8_t *) &link->mac,
                                    sizeof (link->mac), ARPHRD_ETHER);
diff --git a/src/network/networkd-network-gperf.gperf b/src/network/networkd-network-gperf.gperf
index 409df17..9313a3e 100644
--- a/src/network/networkd-network-gperf.gperf
+++ b/src/network/networkd-network-gperf.gperf
@@ -68,6 +68,7 @@ Route.Metric,                           config_parse_route_priority,
 Route.Scope,                            config_parse_route_scope,                       0,                             0
 Route.PreferredSource,                  config_parse_preferred_src,                     0,                             0
 DHCP.ClientIdentifier,                  config_parse_dhcp_client_identifier,            0,                             offsetof(Network, dhcp_client_identifier)
+DHCP.SendClientId,                      config_parse_bool,                              0,                             offsetof(Network, dhcp_send_client_id)
 DHCP.UseDNS,                            config_parse_bool,                              0,                             offsetof(Network, dhcp_use_dns)
 DHCP.UseNTP,                            config_parse_bool,                              0,                             offsetof(Network, dhcp_use_ntp)
 DHCP.UseMTU,                            config_parse_bool,                              0,                             offsetof(Network, dhcp_use_mtu)
diff --git a/src/network/networkd-network.c b/src/network/networkd-network.c
index 4315790..dc46e00 100644
--- a/src/network/networkd-network.c
+++ b/src/network/networkd-network.c
@@ -107,6 +107,7 @@ static int network_load_one(Manager *manager, const char *filename) {
         network->dhcp_use_dns = true;
         network->dhcp_use_hostname = true;
         network->dhcp_use_routes = true;
+        network->dhcp_send_client_id = true;
         network->dhcp_send_hostname = true;
         network->dhcp_route_metric = DHCP_ROUTE_METRIC;
         network->dhcp_client_identifier = DHCP_CLIENT_ID_DUID;
diff --git a/src/network/networkd-network.h b/src/network/networkd-network.h
index 03c3f20..4079ba3 100644
--- a/src/network/networkd-network.h
+++ b/src/network/networkd-network.h
@@ -91,6 +91,7 @@ struct Network {
         bool dhcp_use_mtu;
         bool dhcp_use_hostname;
         DHCPUseDomains dhcp_use_domains;
+        bool dhcp_send_client_id;
         bool dhcp_send_hostname;
         bool dhcp_broadcast;
         bool dhcp_critical;
diff --git a/src/systemd/sd-dhcp-client.h b/src/systemd/sd-dhcp-client.h
index 2b865a8..0dfd57e 100644
--- a/src/systemd/sd-dhcp-client.h
+++ b/src/systemd/sd-dhcp-client.h
@@ -93,6 +93,7 @@ int sd_dhcp_client_set_request_option(sd_dhcp_client *client, uint8_t option);
 int sd_dhcp_client_set_request_address(sd_dhcp_client *client,
                                        const struct in_addr *last_address);
 int sd_dhcp_client_set_request_broadcast(sd_dhcp_client *client, int broadcast);
+int sd_dhcp_client_set_send_client_id(sd_dhcp_client *client, int send);
 int sd_dhcp_client_set_index(sd_dhcp_client *client, int interface_index);
 int sd_dhcp_client_set_mac(sd_dhcp_client *client, const uint8_t *addr,
                            size_t addr_len, uint16_t arp_type);
--
2.5.0
